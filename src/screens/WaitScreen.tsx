import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

type Props = {}

const WaitScreen = (props: Props) => {
  return (
    <View>
      <Text>WaitScreen</Text>
    </View>
  )
}

export default WaitScreen

const styles = StyleSheet.create({})